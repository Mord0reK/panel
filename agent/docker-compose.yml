services:
  agent:
    build: .
    image: monitoring-agent:latest
    container_name: monitoring-agent
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./dane:/app/data/
    environment:
      - BACKEND_URL=ws://serwerek:8080/ws/agent
